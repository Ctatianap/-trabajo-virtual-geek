'use strict'

const checkRestaurant = 128;
const Ana = 2;
const people = 9;
const CheckPerson = (checkRestaurant - Ana) / people;
const totalAna = CheckPerson + Ana;

console.log('Cada una debe pagar: ', CheckPerson, ' y Ana paga: ', totalAna );