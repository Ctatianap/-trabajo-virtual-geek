'use strict'

const name = 'Lola';
const myName = 'Tatiana Pabón';
const nameFriend = 'Pepita Peréz';

if (name === myName || name === nameFriend){
    console.log(`Bienvenida, ${name}`);

}else{
    console.log(`Lo siento ${name}, pero el usuario que has introducido no está registrado`);
};